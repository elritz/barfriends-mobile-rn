import { Text } from "#/src/components/ui/text";
import { View } from "react-native";

enum ProfileType {
  USER = "USER",
  VENUE = "VENUE",
}

const VenueProfile = () => (
  <View>
    <Text>HELLO</Text>{" "}
  </View>
);

export default VenueProfile;
