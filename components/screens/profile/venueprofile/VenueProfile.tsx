import { Text } from '@gluestack-ui/themed'
import { View } from 'react-native'

enum ProfileType {
	USER = 'USER',
	VENUE = 'VENUE',
}

const VenueProfile = () => (
	<View>
		<Text>HELLO</Text>{' '}
	</View>
)

export default VenueProfile
